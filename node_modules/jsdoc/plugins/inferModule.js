exports.astNodeVisitor = {
    visitNode: function(node, e, parser, currentSourceName) {
	block = ["/**",
		 " * @module Test",
		 " * @desc This module contains utilities used for converting Relax NG files to",
		 " * the format required by salve.",
		 " * @author Louis-Dominique Dubeau",
		 " * @license MPL 2.0",
		 " * @copyright 2013-2015 Mangalam Research Center for Buddhist Languages",
		 " */"]
	if (node.comments != undefined) {
	    node.comments[0].value = "* Test";
	    node.comments[0].raw = block.join("\n");
	    console.log(node.comments[0]);
	};
	console.log(node);
	// console.log(node.comments[0].value);
	// console.log(currentSourceName + "\n");
	// console.log(e.filename);
    }
};
